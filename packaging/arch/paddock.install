post_install() {
  echo 'Run "paddock setup" as your desktop user to preview and install system integration.'
}

pre_remove() {
  if systemctl is-enabled --quiet paddock.target 2>/dev/null; then
    echo 'error: run "paddock uninstall" as the configured desktop user before removing the package.' >&2
    return 1
  fi
}
